import 'package:flutter/material.dart';

class AppColors {
  // Primary
  static const primary = Color(0xFF003F2D);
  static const primaryLight = Color(0xFF005A40);
  static const primaryDark = Color(0xFF002D1F);

  // Accent
  static const accent = Color(0xFF00C87A);
  static const accentLight = Color(0xFFE8F5E9);

  // Semantic
  static const success = Color(0xFF00C87A);
  static const successLight = Color(0xFFE8F5E9);
  static const successDark = Color(0xFF00875A);

  static const warning = Color(0xFFF59E0B);
  static const warningLight = Color(0xFFFEFCE8);
  static const warningDark = Color(0xFFCA8A04);

  static const error = Color(0xFFEF4444);
  static const errorLight = Color(0xFFFEF2F2);
  static const errorDark = Color(0xFFDC2626);

  // Neutrals
  static const background = Color(0xFFc6d8cc);
  static const card = Color(0xFFFFFFFF);
  static const surface = Color(0xFFEFF4F1);

  static const textPrimary = Color(0xFF1A2E25);
  static const textSecondary = Color(0xFF4A7C6B);
  static const textTertiary = Color(0xFF8FBDAA);
  static const textDisabled = Color(0xFFCBD5E1);

  static const border = Color(0xFFE2E8F0);
  static const borderLight = Color(0xFFF1F5F9);
}
